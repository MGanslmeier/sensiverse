% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all.R
\name{find_uncertainty_source_neuronet}
\alias{find_uncertainty_source_neuronet}
\title{Identify uncertainty sources via neural network approach}
\usage{
find_uncertainty_source_neuronet(
  res,
  focus,
  grid = list(dropout = c(0, 0.3), layers = c(3, 5), units = c(32, 64), learning_rate =
    c(0.001)),
  split = c(train = 0.6, val = 0.2, test = 0.2),
  epochs = 15,
  batch_size = 256,
  seed = 123,
  verbose = 0
)
}
\arguments{
\item{res}{Tibble from \code{estimate_model_space()}.}

\item{focus}{Character. Focus variable.}

\item{grid}{List of hyperparameters with elements \code{dropout}, \code{layers},
\code{units}, \code{learning_rate}. A small grid is recommended.}

\item{split}{Named numeric vector with \code{train}, \code{val}, \code{test} shares.}

\item{epochs}{Integer training epochs.}

\item{batch_size}{Mini-batch size.}

\item{seed}{Random seed.}

\item{verbose}{Keras verbosity (0/1/2).}
}
\value{
List with method label, focus, and per-class SHAP summaries:
\describe{
  \item{all_signs}{Named list (per outcome) with \code{shap_feature} and \code{shap_dimension}.}
}
}
\description{
Trains separate one-vs-rest neural networks for each outcome class
(\code{"not significant"}, \code{"significant negative"}, \code{"significant positive"})
and computes SHAP importances (via \pkg{vip}), and
aggregates to both features and dimensions.
}
\seealso{
Other estimate-sources: 
\code{\link{find_uncertainty_source_mlogit}()},
\code{\link{find_uncertainty_source_neigh}()}
}
\concept{estimate-sources}
